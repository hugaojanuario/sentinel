apiVersion: v1
kind: Service
metadata:
  name: sentinel-frontend
  namespace: sentinel
spec:
  type: LoadBalancer
  selector:
    app: sentinel-frontend
  ports:
      - port: 80
        targetPort: 80
  